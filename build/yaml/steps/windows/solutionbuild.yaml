# Copyright (c) Microsoft Corporation. All rights reserved.
# Licensed under the MIT License.

# Step Template: Build Windows Binaries

parameters:
  Platform: 'Any CPU'
  Configuraton: 'Release'

steps:

# msbuild InstrumentationEngine.sln
- task: MSBuild@1
  displayName: 'Build solution InstrumentationEngine.sln ${{ parameters.Configuration }}/${{ parameters.Platform }}'
  inputs:
    solution: InstrumentationEngine.sln
    msbuildVersion: 16.0
    platform: ${{ parameters.Platform }}
    configuration: ${{ parameters.Configuration }}